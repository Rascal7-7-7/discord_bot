version: "3.8"

services:
  kura:
    build:
      context: ../../apps/kura
      dockerfile: Dockerfile
    env_file:
      - ../../.env
    restart: unless-stopped

  analysis:
    build:
      context: ../../apps/analysis
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - ../../.env
    restart: unless-stopped
